name: dummy

on: 
    push:
      branches: [main]

jobs:
  dummy:
    name: dummy
    runs-on: ubuntu-latest

    steps:
        - name: dummy
          run: echo "dummy"
          

# name: AWS Secret Manager Sharing

# on: push


# jobs:
#   secret-generator:
#     runs-on: ubuntu-latest
#     permissions:
#         id-token: write 
#         contents: read 
#     outputs:
#       handle: ${{ steps.generate-secret.outputs.handle }}
#     steps:
#     - name: Configure AWS credentials
#       uses: aws-actions/configure-aws-credentials@v4
#       with:
#         role-to-assume: ${{ secrets.AWS_SECRET_MANAGER_ROLE_ARN }}
#         aws-region: ${{ vars.TF_VAR_REGION }}
#     - name: generate secret
#       id: generate-secret
#       shell: bash
#       run: |
#         GENERATED_SECRET=$((RANDOM))
#         echo "::add-mask::$GENERATED_SECRET"
#         SECRET_NAME="github-secret-$(date +%s)-$RANDOM"
#         aws secretsmanager create-secret --name "$SECRET_NAME" --secret-string "$GENERATED_SECRET"
#         echo "handle=$SECRET_NAME" >> "$GITHUB_OUTPUT"
#   secret-consumer:
#     runs-on: ubuntu-latest
#     needs: secret-generator
#     permissions:
#         id-token: write 
#         contents: read 
#     steps:
#     - name: Configure AWS credentials
#       uses: aws-actions/configure-aws-credentials@v4
#       with:
#         role-to-assume: ${{ secrets.AWS_SECRET_MANAGER_ROLE_ARN }}
#         aws-region: ${{ vars.TF_VAR_REGION }}
#     - name: use secret
#       shell: bash
#       run: |
#         SECRET_NAME="${{ needs.secret-generator.outputs.handle }}"
#         RETRIEVED_SECRET=$(aws secretsmanager get-secret-value --secret-id "$SECRET_NAME" --query SecretString --output text)
#         echo "::add-mask::$RETRIEVED_SECRET"
#         echo "We retrieved our masked secret: $RETRIEVED_SECRET"
